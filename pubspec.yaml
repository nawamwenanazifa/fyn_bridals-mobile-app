name: fyn_bridal_app
description: "Fyn Bridals Traditional Wear Mobile Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # Firebase packages for backend functionality
  firebase_core: ^3.6.0           # Core Firebase functionality
  cloud_firestore: ^5.4.4         # Real-time database for chat and orders
  firebase_auth: ^5.3.1           # User authentication
  
  # Chat UI (optional - can be added later)
  # dash_chat_2: ^0.0.21          # Pre-built chat UI (uncomment if needed)
  
  # HTTP requests (if needed)
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Add your assets here
  assets:
      # - assets/images/3.jpg
      # -  assets/images/5.jpg
      # - assets/images/bridal1.png
      # - assets/images/image.png
      - assets/images/logo.png
      - assets/images/booking.png  
      - assets/images/elligance.png
      - assets/images/fit.png 


  # To add custom fonts to your application, add a fonts section here
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: fonts/Poppins-Regular.ttf
  #       - asset: fonts/Poppins-Bold.ttf
  #         weight: 700